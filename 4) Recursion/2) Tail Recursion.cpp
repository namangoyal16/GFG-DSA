Tail Recursion happens when function doesn't have anything more to execute after its child function is completely executed ... Tail recursive function has less time complexity in modern compilers ..

The last thing which happens in a tail recursion is to execute the last recursice call.
